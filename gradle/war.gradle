apply plugin: "war"

bootWar {
    mainClassName = "org.dshakes.musicbrainz.searchservice.MusicbrainzsearchApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
